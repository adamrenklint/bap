var chai = require('chai');
var expect = chai.expect;
var Channel = require('../../lib/Channel');

var channel;

beforeEach(function () {
  channel = new Channel();
});

describe('Channel', function () {

});
